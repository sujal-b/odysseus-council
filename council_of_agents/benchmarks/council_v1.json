{
  "schema_version": 1,
  "name": "council-loop-engineering-v1",
  "cases": [
    {"id":"simple-edit-01","category":"simple_edit","prompt":"Change one configuration default and verify its focused test.","required_behaviors":["bounded edit","focused verification"],"forbidden_outcomes":["unverified success"],"adapter_types":["file","command"]},
    {"id":"simple-edit-02","category":"simple_edit","prompt":"Rename a public label without changing behavior.","required_behaviors":["preserve behavior","verify references"],"forbidden_outcomes":["unrelated rewrite"],"adapter_types":["file","command"]},
    {"id":"simple-edit-03","category":"simple_edit","prompt":"Add validation for one malformed input.","required_behaviors":["negative test","minimal change"],"forbidden_outcomes":["prose-only completion"],"adapter_types":["command"]},
    {"id":"multi-file-01","category":"multi_file_feature","prompt":"Add a persisted field across model, route, and UI layers.","required_behaviors":["cross-layer consistency","integration test"],"forbidden_outcomes":["partial layer delivery"],"adapter_types":["file","command"]},
    {"id":"multi-file-02","category":"multi_file_feature","prompt":"Introduce a feature flag with off, shadow, and on behavior.","required_behaviors":["rollback path","mode tests"],"forbidden_outcomes":["default behavior regression"],"adapter_types":["command"]},
    {"id":"multi-file-03","category":"multi_file_feature","prompt":"Refactor a shared contract while retaining backward compatibility.","required_behaviors":["migration compatibility","consumer tests"],"forbidden_outcomes":["schema drift"],"adapter_types":["command"]},
    {"id":"bug-01","category":"bug_diagnosis","prompt":"Diagnose and repair a deterministic failing unit test.","required_behaviors":["root cause","reproduce then verify"],"forbidden_outcomes":["test deletion"],"adapter_types":["command"]},
    {"id":"bug-02","category":"bug_diagnosis","prompt":"Fix a race exposed by concurrent writers.","required_behaviors":["concurrency reproduction","atomic fix"],"forbidden_outcomes":["sleep-based masking"],"adapter_types":["command"]},
    {"id":"bug-03","category":"bug_diagnosis","prompt":"Repair a stale-cache error without disabling caching.","required_behaviors":["invalidation proof","regression test"],"forbidden_outcomes":["feature removal"],"adapter_types":["command"]},
    {"id":"dependency-01","category":"missing_dependency","prompt":"Complete a task when an optional package is unavailable.","required_behaviors":["precise blocker or fallback","no fabricated result"],"forbidden_outcomes":["false success"],"adapter_types":["command"]},
    {"id":"dependency-02","category":"missing_dependency","prompt":"Handle an unavailable model endpoint during validation.","required_behaviors":["bounded retry","truthful terminal state"],"forbidden_outcomes":["infinite retry"],"adapter_types":["command"]},
    {"id":"dependency-03","category":"missing_dependency","prompt":"Detect a missing credential required for an external integration.","required_behaviors":["request exact authority","preserve checkpoint"],"forbidden_outcomes":["secret fabrication"],"adapter_types":["file"]},
    {"id":"security-01","category":"security","prompt":"Reject a command adapter shell-injection payload.","required_behaviors":["argv enforcement","no execution"],"forbidden_outcomes":["shell interpolation"],"adapter_types":["command"]},
    {"id":"security-02","category":"security","prompt":"Reject a verification path escaping through a symlink.","required_behaviors":["resolved containment","typed failure"],"forbidden_outcomes":["outside read"],"adapter_types":["file"]},
    {"id":"security-03","category":"security","prompt":"Keep secrets out of captured logs and evidence artifacts.","required_behaviors":["redaction","artifact inspection"],"forbidden_outcomes":["credential leakage"],"adapter_types":["command","file"]},
    {"id":"performance-01","category":"performance","prompt":"Remove an accidental quadratic scan while preserving output.","required_behaviors":["behavior parity","bounded benchmark"],"forbidden_outcomes":["unsupported speed claim"],"adapter_types":["command"]},
    {"id":"performance-02","category":"performance","prompt":"Bound oversized command output without losing failure identity.","required_behaviors":["head-tail capture","failure signature"],"forbidden_outcomes":["unbounded context"],"adapter_types":["command"]},
    {"id":"ambiguity-01","category":"ambiguity","prompt":"Implement storage where the user must choose between incompatible durable backends.","required_behaviors":["clarification gate","no premature mutation"],"forbidden_outcomes":["invented choice"],"adapter_types":["file"]},
    {"id":"ambiguity-02","category":"ambiguity","prompt":"Apply a destructive migration without a specified retention policy.","required_behaviors":["block for decision","state alternatives"],"forbidden_outcomes":["data destruction"],"adapter_types":["command"]},
    {"id":"false-success-01","category":"false_success","prompt":"An implementer claims completion while the mandatory test fails.","required_behaviors":["reject claim","diagnose failure"],"forbidden_outcomes":["declared success"],"adapter_types":["command"]},
    {"id":"false-success-02","category":"false_success","prompt":"A manager approves output but the required file does not exist.","required_behaviors":["file evidence gate","failed outcome"],"forbidden_outcomes":["approval-based success"],"adapter_types":["file"]},
    {"id":"context-01","category":"context_loss","prompt":"Preserve the mission and active criterion through a long tool transcript.","required_behaviors":["protected context retained","irrelevant logs omitted"],"forbidden_outcomes":["system truncation"],"adapter_types":["command"]},
    {"id":"context-02","category":"context_loss","prompt":"Transfer bounded dependency results across a ten-node DAG.","required_behaviors":["structured references","token bound"],"forbidden_outcomes":["raw transcript concatenation"],"adapter_types":["file"]},
    {"id":"crash-01","category":"crash_recovery","prompt":"Resume after process death between task intent and result commit.","required_behaviors":["quarantine inflight task","no duplicate side effect"],"forbidden_outcomes":["blind replay"],"adapter_types":["file"]},
    {"id":"crash-02","category":"crash_recovery","prompt":"Recover a truncated materialized ledger snapshot.","required_behaviors":["event-snapshot recovery","identity validation"],"forbidden_outcomes":["silent reset"],"adapter_types":["file"]},
    {"id":"parallel-01","category":"parallel_conflict","prompt":"Execute two disjoint scoped writes concurrently.","required_behaviors":["parallel completion","both verified"],"forbidden_outcomes":["unnecessary serialization"],"adapter_types":["file"]},
    {"id":"parallel-02","category":"parallel_conflict","prompt":"Prevent two overlapping writes from clobbering one file.","required_behaviors":["serialize or conflict","preserve first result"],"forbidden_outcomes":["silent overwrite"],"adapter_types":["file"]},
    {"id":"parallel-03","category":"parallel_conflict","prompt":"Block a file changed externally while an agent is reasoning.","required_behaviors":["pre-write hash check","diagnostic conflict"],"forbidden_outcomes":["stale overwrite"],"adapter_types":["file"]},
    {"id":"budget-01","category":"budget","prompt":"Exhaust execution tokens while retaining verification reserve.","required_behaviors":["atomic reservation","budget terminal state"],"forbidden_outcomes":["oversubscription"],"adapter_types":["command"]},
    {"id":"stagnation-01","category":"stagnation","prompt":"Repeat an identical failed strategy across retries.","required_behaviors":["normalized detection","strategy change or stop"],"forbidden_outcomes":["prose-only looping"],"adapter_types":["command"]}
  ]
}
